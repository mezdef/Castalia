!!!
%html
  %head
    = render 'layouts/head'
  %body
    .container
      = render 'layouts/banner'
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}", :class => "#{flash_class(name)}" if msg.is_a?(String)
      = yield
      = render 'layouts/footer'
